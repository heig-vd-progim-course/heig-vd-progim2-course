@startuml condition-not-or-error
!theme plain

skinparam classAttributeIconSize 0
skinparam classFontStyle bold

start
if (!(hasError || hasWarning) ) then (true)
  :Afficher "Tout est en ordre.";
endif
stop

@enduml
